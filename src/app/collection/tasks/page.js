import SettingsHeader from "@/components/settings/SettingsHeader";

export default function CollectionTasksPage() {
  return (
    <SettingsHeader title="Tasks" description="Manage all tasks in one place">
      <div>tasks</div>
    </SettingsHeader>
  );
}
