import SettingsHeader from "@/components/settings/SettingsHeader";

export default function CollectionRoutinesPage() {
  return (
    <SettingsHeader
      title="Routines"
      description="Manage all routines in one place"
    >
      <div>routines</div>
    </SettingsHeader>
  );
}
